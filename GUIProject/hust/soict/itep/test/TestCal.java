package hust.soict.itep.test;

import hust.soict.itep.swing.NumberGrid;

public class TestCal {
    public static void main(String[] args) {
        NumberGrid numGrid = new NumberGrid();
        numGrid.setTitle("Number Grid");
        numGrid.setSize(300, 400);
        numGrid.setVisible(true);
    }
}
